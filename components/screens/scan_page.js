import styles from "../../styles";
import { Text, View } from 'react-native';

export default function ScanPage() {
    return (
        <View style={styles.container}>
      <Text>ScanPage Screen</Text>
    </View>
    );
}